# ==============================================================================
# Tailor IS - Academy Service Deployment + Service
# ==============================================================================
# 学堂服务 - /api/academy/**
# 端口: 8112 | 副本: 2
# ==============================================================================

apiVersion: v1
kind: Service
metadata:
  name: academy-service
  namespace: tailor-is-prod
  labels:
    app: tailor-is
    component: academy-service
    environment: production
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8112
      targetPort: 8112
      protocol: TCP
  selector:
    app: tailor-is
    component: academy-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: academy-service
  namespace: tailor-is-prod
  labels:
    app: tailor-is
    component: academy-service
    environment: production
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: tailor-is
      component: academy-service
  template:
    metadata:
      labels:
        app: tailor-is
        component: academy-service
        environment: production
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8112"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: academy-service
          image: tailor-is/academy-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8112
              protocol: TCP
          env:
            - name: SERVER_PORT
              valueFrom:
                configMapKeyRef:
                  name: tailor-is-config
                  key: ACADEMY_SERVICE_PORT
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: tailor-is-config
                  key: SPRING_PROFILES_ACTIVE
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: tailor-is-config
                  key: MYSQL_ACADEMY_URL
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx256m -XX:+UseG1GC -XX:MaxGCPauseMillis=200"
          envFrom:
            - configMapRef:
                name: tailor-is-config
            - secretRef:
                name: tailor-is-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "768Mi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8112
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8112
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8112
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          volumeMounts:
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30