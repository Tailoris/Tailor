# 手机\+邮箱双渠道注册登录 前端开发任务

## 总任务分类

1. 注册页改造（双 Tab：手机号注册 / 邮箱注册）

2. 登录页改造（密码登录、短信登录双 Tab 兼容手机 / 邮箱账号）

3. 全局通用组件 / 工具改造（验证码倒计时、表单校验、弹窗、提示、接口封装）

4. 配套页面联动（忘记密码页同步适配双渠道）

5. 交互边界、埋点、UI 兼容、自测用例

---

## 一、注册页面开发任务（Register\.vue）

### 任务 1：顶部 Tab 栏新增切换逻辑

1. 布局新增 `手机号注册 / 邮箱注册` 双 Tab 标签，默认激活「手机号注册」

2. 编写 Tab 切换状态管理变量 `activeRegisterTab`：`phone` / `email`

3. Tab 视觉样式复用现有规范：激活态蓝色文字 \+ 底部蓝下划线，未激活灰色文字

4. 切换 Tab 前置弹窗逻辑：

    - 判断当前 Tab 任意输入框有值 → 弹出确认弹窗「切换注册方式将清空当前已填内容，是否继续？」

    - 弹窗按钮：取消（留在当前 Tab）、确认（切换 Tab \+ 清空当前表单所有输入、清空校验报错）

5. 全局共用协议勾选状态：`isAgree` 变量跨 Tab 不重置，切换 Tab 保留勾选 / 取消状态

### 任务 2：Tab1 手机号注册表单（原有页面微调，无结构改动）

1. 输入框占位符优化：手机号框改为 `请输入11位中国大陆手机号`

2. 统一表单校验规则封装，新增实时失焦校验

    - 手机号：11 位数字正则校验，错误红色提示

    - 验证码：6 位数字长度校验

    - 密码：长度≥6 位校验，增加弱密码友好提示（不拦截）

    - 确认密码：和密码输入值全等校验

3. 发送验证码按钮逻辑复用原有短信验证码接口，增加前置表单校验（手机号合法才可点击）

4. 倒计时组件绑定短信渠道，60s 置灰不可点击

### 任务 3：Tab2 新增【邮箱注册】完整表单

1. 新增邮箱账号输入框，图标复用原有通讯图标，占位符：`请输入常用邮箱账号`

2. 右侧复用「发送验证码」按钮，绑定邮箱验证码下发接口

3. 邮箱正则校验（xxx@xx\.xx），失焦实时校验，非法格式拦截验证码发送

4. 新增邮箱验证码输入框，占位符：`请输入邮箱收到的6位验证码`，6 位数字校验

5. 密码、确认密码输入框完全复用手机号注册表单组件，校验逻辑 1:1 统一

6. 密码规则：8字符以上，含数字＋英文＋特殊字符

7. 注册按钮禁用逻辑：

    - 邮箱、邮箱验证码、密码、确认密码任意一项为空 / 格式错误 → 按钮置灰

    - 协议未勾选 → 永久置灰不可点击

8. 点击注册触发邮箱注册接口，成功后跳转登录页 \+ 顶部成功 Toast 提示

### 任务 4：注册页底部通用模块（双 Tab 共用）

1. 底部跳转文字「已有账号？立即登录」绑定路由跳转登录页，Tab 切换不销毁

2. 协议弹窗链接：《用户协议》《隐私政策》点击弹窗展示文档，全局共用弹窗组件

3. 注册按钮防重复点击：点击后 1s 内禁用，避免重复提交注册 / 重复发验证码

### 任务 5：注册页接口对接

1. 手机号注册接口：原有接口不变，表单参数正常传

2. 新增邮箱注册接口：封装统一请求方法，区分渠道 type=email

3. 短信验证码下发接口（type=phone）、邮箱验证码下发接口（type=email）做统一封装

---

## 二、登录页面开发任务（Login\.vue）

### 任务 1：登录 Tab 栏（密码登录 / 短信登录）保留原有结构，改造表单兼容双渠道

1. 保留 `activeLoginTab`：`pwd`（默认激活） / `code`

2. Tab 切换逻辑：切换清空当前 Tab 输入、清空校验报错，无确认弹窗

### 任务 2：Tab1 密码登录（核心改造：账号输入框支持手机号 / 邮箱 / 用户名）

1. 修改账号输入框占位符：`用户名 / 手机号 / 邮箱`

2. 账号输入实时识别逻辑：

    - 输入纯 11 位数字 → 识别为手机号

    - 输入包含`@`符号 → 识别为邮箱

    - 其他文本 → 识别为用户名

3. 账号格式校验：

    - 手机号：非法号段 / 非 11 位数字红色报错

    - 邮箱：不匹配邮箱正则红色报错

4. 密码输入框保留原有逻辑，非空校验

5. 辅助区改造：

    - 左侧「记住我」复选框：勾选后本地 storage 缓存账号（仅账号，不存密码），页面初始化自动回填

    - 右侧「忘记密码？」路由跳转找回密码页面

6. 登录按钮校验：账号、密码均非空才可点击，调用统一密码登录接口（后端自动识别账号类型）

7. 登录失败统一提示：`账号或密码错误，请重新输入`，不区分账号不存在 / 密码错误

### 任务 3：Tab2 短信登录（改造：账号框兼容手机 / 邮箱，自动分发验证码渠道）

1. 修改账号输入框占位符：`请输入已注册手机号/邮箱`

2. 发送验证码按钮前置逻辑：

    - 输入合法手机号 → 调用短信验证码接口

    - 输入合法邮箱 → 调用邮箱验证码接口

    - 格式错误直接提示，按钮置灰不可点击

3. 验证码输入框复用原有组件，6 位数字校验

4. 登录按钮：账号 \+ 验证码填写完整才可点击，调用免密验证码登录接口

5. 倒计时组件自动区分渠道，短信 / 邮箱共用倒计时 UI

### 任务 4：登录页底部通用模块

1. 底部「还没有账号？立即注册」跳转注册页，双 Tab 共用

2. 所有提交按钮增加 1s 防重复点击拦截

### 任务 5：登录页接口对接

1. 密码登录接口：入参不变，后端兼容手机号 / 邮箱 / 用户名三种账号查询

2. 验证码免密登录接口：新增渠道标识 type，区分 phone/email

3. 统一验证码下发工具方法，根据输入账号类型自动判断调用短信 / 邮箱接口

---

## 三、全局通用组件 \& 工具层改造（公共模块，所有页面复用）

### 任务 1：验证码倒计时组件 CountDown

1. 抽离渠道参数 `sendType: phone | email`，兼容短信、邮箱两种下发逻辑

2. 统一 UI 样式、倒计时逻辑、置灰状态，注册 / 登录 / 找回密码页面全局复用

### 任务 2：全局表单校验工具 utils/validate\.js

新增 / 优化正则校验方法，全局导出：

1. `isPhone()`：11 位中国大陆手机号校验

2. `isEmail()`：通用邮箱格式正则校验

3. `isSixCode()`：6 位数字验证码校验

4. `isValidPwd()`：密码长度≥6 位校验

5. `isEqualPwd()`：两次密码一致性对比
所有页面统一调用该工具，保证报错文案、校验规则完全一致

### 任务 3：请求封装 api/auth\.js

统一封装认证相关接口，新增邮箱相关方法：

1. sendCode \(账号，type\)：自动分发短信 / 邮箱验证码

2. registerPhone \(\)：手机号注册

3. registerEmail \(\)：邮箱注册

4. loginByPwd \(\)：密码登录（兼容多账号类型）

5. loginByCode \(\)：验证码免密登录（区分渠道）

6. resetPwd \(\)：找回密码接口（兼容手机 / 邮箱）

### 任务 4：全局轻提示、弹窗组件

1. Toast 成功 / 失败提示统一文案规范

2. 确认弹窗组件封装「切换表单清空确认弹窗」，注册页复用

### 任务 5：本地存储工具 storage\.js

新增记住我缓存方法，仅存储账号字符串，不保存敏感密码信息

---

## 四、配套页面：忘记密码（ResetPwd\.vue）同步适配双渠道

1. 账号输入框支持手机号 / 邮箱输入，自动识别渠道下发验证码

2. 表单校验复用全局 validate 工具

3. 验证码倒计时组件复用全局 CountDown，区分 phone/email

4. 重置密码成功后跳转登录页，提示重置完成

---

## 五、交互、UI、兼容性、自测任务

### 1\. UI 统一约束任务

1. 所有新增输入框、按钮、Tab 样式严格沿用现有页面圆角、边框、主蓝色色值，不新增设计样式

2. 报错提示统一红色小字，位置固定在输入框下方，输入聚焦自动清除报错

3. Tab 切换无额外动画，仅状态文字 / 下划线切换，和现有交互体验保持一致

### 2\. 边界交互自测任务（开发完成后逐项自测）

#### 注册页自测点

1. 手机号 Tab 空表单点击注册：按钮置灰，无请求

2. 邮箱 Tab 填写一半内容切换 Tab：弹出确认，确认后清空表单

3. 邮箱格式错误，无法点击发送验证码

4. 两次密码不一致，点击注册红色报错

5. 取消协议勾选，注册按钮永久禁用

6. 验证码倒计时期间按钮不可重复点击

7. 手机号注册、邮箱注册成功均正常跳转登录页

#### 登录页自测点

1. 密码登录输入邮箱账号，可正常校验登录

2. 短信登录输入邮箱，点击发送验证码自动下发邮箱验证码

3. 记住我勾选后刷新页面，账号自动回填

4. 错误账号 / 密码登录，统一安全提示，不泄露账号是否存在

5. 验证码错误登录，展示对应提示

#### 通用边界

1. 快速重复点击发送验证码、注册、登录按钮，无重复接口请求

2. 非法字符（特殊符号、空格）输入账号框实时拦截报错

3. 页面路由来回跳转（注册↔登录↔找回密码）表单状态正常销毁 / 重置



> （注：文档部分内容可能由 AI 生成）
