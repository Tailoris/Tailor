# ==============================================================================
# Tailor IS - Community Service Deployment + Service
# ==============================================================================
# 社区服务 - /api/community/**
# 端口: 8108 | 副本: 2
# ==============================================================================

apiVersion: v1
kind: Service
metadata:
  name: community-service
  namespace: tailor-is-prod
  labels:
    app: tailor-is
    component: community-service
    environment: production
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8108
      targetPort: 8108
      protocol: TCP
  selector:
    app: tailor-is
    component: community-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-service
  namespace: tailor-is-prod
  labels:
    app: tailor-is
    component: community-service
    environment: production
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: tailor-is
      component: community-service
  template:
    metadata:
      labels:
        app: tailor-is
        component: community-service
        environment: production
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8108"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: community-service
          image: tailor-is/community-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8108
              protocol: TCP
          env:
            - name: SERVER_PORT
              valueFrom:
                configMapKeyRef:
                  name: tailor-is-config
                  key: COMMUNITY_SERVICE_PORT
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: tailor-is-config
                  key: SPRING_PROFILES_ACTIVE
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: tailor-is-config
                  key: MYSQL_COMMUNITY_URL
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200"
          envFrom:
            - configMapRef:
                name: tailor-is-config
            - secretRef:
                name: tailor-is-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1024Mi"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8108
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8108
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8108
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          volumeMounts:
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30