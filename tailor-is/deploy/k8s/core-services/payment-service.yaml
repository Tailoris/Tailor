apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: tailor-is-prod
  labels:
    app: payment
    app.kubernetes.io/name: tailor-is-payment
spec:
  type: ClusterIP
  selector:
    app: payment
  ports:
    - name: http
      port: 8104
      targetPort: 8104
      protocol: TCP
