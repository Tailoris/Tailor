<?xml version="1.0" encoding="UTF-8"?>
<!--
  🔒 B-L13修复: 日志按天滚动 + 大小切割 + 保留天数配置
  - TimeBasedRollingPolicy: 按天滚动归档
  - SizeAndTimeBasedFNATP: 单文件 100MB 触发切割
  - maxHistory=30: 保留30天日志
  - totalSizeCap=20GB: 总日志大小上限，避免磁盘占满
  - 增加 MDC traceId 占位符（L-21 配合）
  - 增加 ERROR 独立追加器（重要错误单独归档）
-->
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name"/>

    <!-- 🔒 B-L13修复: 日志保留天数（可被启动参数覆盖：-Dlog.max-history=60） -->
    <property name="LOG_MAX_HISTORY" value="30"/>
    <!-- 单个日志文件最大大小 -->
    <property name="LOG_MAX_FILE_SIZE" value="100MB"/>
    <!-- 日志总磁盘占用上限 -->
    <property name="LOG_TOTAL_CAP" value="20GB"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- 🔒 B-L13修复: 增加 traceId MDC 占位符，便于分布式链路追踪 -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 🔒 B-L13修复: 按天滚动 + 单文件大小切割的文件追加器 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${appName}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 按天滚动: logs/tailor-is-user.2026-06-03.0.log -->
            <fileNamePattern>logs/${appName}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LOG_TOTAL_CAP}</totalSizeCap>
            <cleanHistoryOnStart>false</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 🔒 B-L13修复: ERROR 级别日志独立归档，便于快速定位严重错误 -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${appName}-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/${appName}-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
            <totalSizeCap>${LOG_TOTAL_CAP}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId=%X{traceId:-}] %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 业务日志级别控制 -->
    <logger name="com.tailoris" level="DEBUG"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="com.baomidou.mybatisplus" level="WARN"/>
    <logger name="org.apache.ibatis" level="WARN"/>
    <logger name="com.alibaba.nacos" level="WARN"/>
    <logger name="org.apache.dubbo" level="WARN"/>
    <logger name="com.netflix" level="WARN"/>

    <!-- 默认root配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- 开发/测试环境: 输出到控制台，DEBUG级别 -->
    <springProfile name="dev,test">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <!-- 预发环境: 控制台+文件 -->
    <springProfile name="staging">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
            <appender-ref ref="FILE_ERROR"/>
        </root>
    </springProfile>

    <!-- 生产环境: 仅文件，WARN级别 -->
    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
            <appender-ref ref="FILE_ERROR"/>
        </root>
    </springProfile>
</configuration>
